{
  "name": "adjustments-builder",
  "private": true,
  "description": "Build and test tooling for the Adjustments Builder. The app itself has no dependencies.",
  "license": "CC-BY-NC-SA-4.0",
  "type": "module",
  "scripts": {
    "build": "python3 tools/build.py",
    "check": "python3 tools/build.py --check",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
